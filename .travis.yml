language: java
jdk:
  - openjdk8

branches:
  only:
    - master

before_install:
  - chmod +x gradlew

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r egs41-springboot-webservice *
  - mkdir -p deploy
  - mv egs41-springboot-webservice.zip deploy/egs41-springboot-webservice.zip

deploy:
  - provider: s3
    access_key_id: AKIA6LB7AOAOGCY343UO # Travis repo settings에 설정된 값
    secret_access_key: cASyeP8MqdVlIJBJ6RVPofgxKF91VcfA9i1gEvMi # Travis repo settings에 설정된 값
    bucket: egs41-springboot-build # S3 버킷
    region: ap-northeast-2
    skip_cleanup: true
    acl: private
    local_dir: deploy
    wait-until-deployed: true

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - egs41@naver.com